# PasswordGuard
PasswordGuard started from something dumb and relatable — typing a weak password, seeing it go "green," and moving on with zero clue why. So we built a tool that actually explains itself: real-time checks for weak patterns, common passwords, and repeats — no APIs, no backend, nothing leaves your browser.
